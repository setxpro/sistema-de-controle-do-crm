import styled from "styled-components";

export const ContainerAuthors = styled.div`
  position: absolute;
  bottom: 10px;
  right: 10px;

  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 1rem;

  p,
  a {
    color: #999;
  }
  p {
    a {
    }
  }
`;
